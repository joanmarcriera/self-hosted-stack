# Handoff — 2026-09-05

## What changed
- Added `CLAUDE.md` (purpose, rebuild run/test steps, layout, AGENTS.md conventions, gotchas, key
  references).
- `README.md`: added a short usage-header paragraph + rebuild pointer (2 sentences), preserving
  the rest.

## Verified against the actual code
- `opt-stacks/{core,platform,identity,automation,business,monitoring}` and `docs/*.md` layout
  matches what CLAUDE.md describes.
- `scripts/server/*.sh` filenames referenced in CLAUDE.md all exist.
- `.gitignore` confirms `.secrets/` and `reports/` are excluded, matching the "gitignored" notes.
- **Fixed an invented detail**: the first draft stated the Grafana dashboard lives at
  `https://grafana.joanmarcriera.es`. That hostname appears nowhere in the repo — the actual
  compose config (`opt-stacks/monitoring/docker-compose.yml`) uses a `STATUS_HOST` env var, whose
  `.env.example` placeholder is `status.example.com`; the real live value is set in the
  (gitignored) `.env` and isn't committed. Reworded to describe the mechanism instead of guessing
  the live hostname.

## Left to do
Nothing outstanding. No secrets found in either doc.
